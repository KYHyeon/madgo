class labels {
    data = [];

    constructor() {
        this.data = this.readJson()
    }

    readJson() {
        // TODO 클라이언트단에서 파일입출력이 가능한가???
        // TODO 읽어서 data에 집어넣자

        // var fs = require('fs');
        // JSON.parse()
    }
}